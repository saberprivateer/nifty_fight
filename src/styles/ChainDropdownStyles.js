import { StyleSheet } from 'react-native';

const chainDropdownStyles = StyleSheet.create({
  // While the dropdown component is using TailwindCSS directly for styling,
  // if specific React Native styling were needed, it would be defined here.
  // For this web-based project, Tailwind classes are used directly in JSX.
});

export default chainDropdownStyles;
